apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: xss-lab-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: xss-lab
            port:
              number: 80
